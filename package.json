{
  "name": "firestore-model",
  "version": "0.0.19",
  "description": "Firestore / Firebase object model schema wrapper",
  "keywords": [
    "Firestore",
    "Firebase",
    "object",
    "model",
    "schema"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/joonhocho/firestore-model.git"
  },
  "bugs": {
    "url": "https://github.com/joonhocho/firestore-model/issues"
  },
  "homepage": "https://github.com/joonhocho/firestore-model#readme",
  "author": "Joon Ho Cho",
  "license": "MIT",
  "main": "es6/index.js",
  "module": "es6/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p ./tsconfig.json && tscpaths -p ./tsconfig.json -s ./src -o ./lib",
    "build:es6": "tsc -p ./tsconfig.es6.json && tscpaths -p ./tsconfig.es6.json -s ./src -o ./es6",
    "clear": "rm -rf ./lib ./es6 ./coverage",
    "format": "prettier --write \"./*.{js,jsx,ts,tsx}\" \"./src/**/*.{js,jsx,ts,tsx}\"",
    "lint": "tslint -c ./tslint.json \"src/**/*\"",
    "lint:fix": "tslint --fix -c ./tslint.json \"src/**/*\"",
    "start": "yarn clear && yarn format && yarn lint:fix && yarn test && yarn build && yarn build:es6",
    "prepublish": "yarn start",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.11.3",
    "firebase-admin": "7.1.0",
    "jest": "^24.5.0",
    "prettier": "^1.16.4",
    "ts-jest": "^24.0.0",
    "tscpaths": "^0.0.6",
    "tslint": "^5.14.0",
    "tslint-config-airbnb": "^5.10.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.3.3333"
  },
  "peerDependencies": {
    "firebase-admin": "7.1.0"
  },
  "engines": {
    "node": ">=8.2.1"
  },
  "engineStrict": true
}
